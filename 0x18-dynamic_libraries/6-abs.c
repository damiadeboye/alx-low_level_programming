#include "main.h"

/**
 * _abs - print the absolute value of an integer
 * @n: n is an integer
 *
 * Return: 0 if successfull
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
